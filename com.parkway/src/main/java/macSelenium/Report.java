package macSelenium;

import org.apache.commons.mail.DefaultAuthenticator;
import org.apache.commons.mail.Email;
import org.apache.commons.mail.EmailException;
import org.apache.commons.mail.SimpleEmail;

public class Report {
	
	public static void main(String[] args) throws EmailException{
		
	/*	Email email = new SimpleEmail();
		email.TCPIP("notes.na.collabserv.com ");
		email.setSmtpPort(1533);
		email.setAuthenticator(new DefaultAuthenticator("shivsh@in.ibm.com", "friday99"));
		email.setSSLOnConnect(true);
		email.setFrom("shivsh78@in.ibm.com");
		email.setSubject("automation report");
		email.setMsg("This is a test mail for report");
		email.addTo("lathasri.t@in.ibm.com");
		email.send();
		*/
		
	}}